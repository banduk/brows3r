# Spec Review: s3-native-browser

Verdict: APPROVE
Findings: 0

## Findings

### Blocker Findings

None.

### Major Findings

None.

### Minor Findings

None. Both round-3 minors are resolved exactly as suggested and no new issues were introduced.

## Round-3 Traceability

| # | Round-3 minor | Verification in round 4 | Status |
|---|---|---|---|
| 1 | [consistency] Decisions ledger pointed to non-existent `## Default Values` (line 53). | Line 53 now reads: "v1 default values are codified inline in `## Settings (with v1 defaults)` so acceptance criteria can be tested out of the box." The named section exists at line 190 (`**Settings** (with v1 defaults):`) with the full enumerated default block running through line 206. The pointer now resolves. | Resolved |
| 2a | [testability] AC-6 line 295 said "the configured preview size limit". | Line 295 now reads: "Given the user selects a file over the default 50 MB preview size limit, then a warning is shown with an option to download instead." Matches the 50 MB value codified at Settings line 194 ("Preview file size limit (default: 50 MB — warn/skip for larger)"). Mirrors the AC-4/AC-8 pattern. | Resolved |
| 2b | [testability] AC-13 line 333 said "the configured download+upload fallback threshold". | Line 333 now reads: "Given the user performs a cross-account copy, when S3 ACLs would block server-side copy, then the app follows the default 100 MB download+upload fallback threshold, notifies the user, and asks for confirmation when required." Matches the 100 MB value at Settings line 198 and the AC-4 wording on lines 275-276 ("below the default 100 MB fallback threshold" / "above the default 100 MB fallback threshold"). Tests are now reproducible against fresh-install defaults end-to-end. | Resolved |

### New Issues Introduced in Round 4

None. The three edits are localized text changes to one Decision bullet and two AC sentences. Cross-checked against:

- Settings defaults block (lines 190-206): 50 MB and 100 MB values unchanged and now consistently referenced.
- Architecture cache/lock/provider sections (lines 71-167): unaffected.
- Other ACs (AC-1 through AC-14): no collateral edits, no new inconsistencies.
- Phase plan (Phases 1-5): unaffected.
- Risks table (lines 412-430): cross-account fallback risk and cost surprise risk still align with the now-defaulted threshold.
- Non-goals (lines 230-241): unaffected.
- Decisions ledger entries 1-26 (lines 25-53): only line 53 changed; the other 25 decisions still hold and do not contradict the new wording.

No "configured" wording remains in any AC for thresholds or limits that have a v1 default. The remaining uses of "configured/configurable" in the document (Decisions line 34, Architecture line 108, Architecture line 157, Architecture line 167, Settings line 195, AC-1 line 248, Risks line 419) are correct in context — they describe configurability of values that are independently codified with their defaults in the Settings block, and they are not acceptance criteria.

## Summary

Round 4 cleanly resolves both round-3 minor findings. The Decisions ledger entry on line 53 now points to the actually-existing `## Settings (with v1 defaults)` section instead of the phantom `## Default Values`. AC-6 line 295 and AC-13 line 333 both now name the concrete default values (50 MB preview limit, 100 MB cross-account fallback threshold), matching the wording pattern already established by AC-4 lines 275-276 and AC-8 lines 303 and 306. The three edits are surgical, internally consistent with the Settings defaults block (lines 190-206), and do not contradict any other Decision, Architecture, AC, Phase, or Risk entry. No new findings of any severity. Proposal is ready for planner handoff.

## Checklist By Category

- clarity: checked
- completeness: checked
- testability: checked
- consistency: checked
- risk: checked

## Residual Risk

None at proposal level. The two implementation-time watch items noted in round 3 still apply and remain acceptable for the planner to absorb during design without revisiting the proposal:

1. The diff-preview framework has a concrete v1 trigger (storage class change) but no AC covers cancel-from-diff-preview behavior. Planner can fold that into the framework's design.
2. Column view selection behavior on entry from a non-Column mode is implied (location preserved, deeper-column selection resets) but not separately tested in AC-3 for cross-mode entry into Column view. Acceptable for v1; planner may add a derived test case during design.

## Next Step

Hand off `proposal.md` to the planner. No further `/review-spec` round is required. The proposal is clear, complete, testable out-of-the-box against documented defaults, internally consistent, and risk-managed.
