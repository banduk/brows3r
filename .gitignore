# Tolaria app files (machine-specific, never commit)
.laputa/settings.json

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# Editors
.vscode/
.idea/
*.swp
*.swo

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node / Vite build output
node_modules
dist
dist-ssr
*.local

# Auto-synced from pdfjs-dist by scripts/sync-pdf-worker.mjs (postinstall).
# Version-pinned to the installed pdfjs-dist; never commit.
public/pdf.worker.min.mjs

# VitePress + TypeDoc + rustdoc output, regenerated by docs.yml on every push.
dist-docs/
docs/.vitepress/cache/
docs/.vitepress/dist/

# Rust / Cargo (root-level ignored; src-tauri/.gitignore handles src-tauri/target)
target/


/tmp.claude/
